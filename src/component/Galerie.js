import style from './Galerie.module.scss';

function Galerie() {
    return (
        <div className={` ${style.galerie}`} >
            <h1>Galerie</h1>
        </div>
    )
}

export default Galerie;